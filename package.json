{
  "name": "abhyasa",
  "version": "0.1.3",
  "description": "Open-source modular quiz and assessment engine",
  "private": true,
  "type": "module",
  "scripts": {
    "sitemap": "node generate-sitemap.js",
    "theme": "sass --quiet --style=compressed src/css/theme.scss src/css/theme.css",
	"clean": "rm -rf dist/* && cp -r public/* dist/",
    "build": "npm run theme && npm run clean && node esbuild.config.js",
    "dev": "npm run theme && npm run clean && node esbuild.config.js --dev"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.27.2"
  },
  "dependencies": {
    "bootstrap": "^5.3.8",
    "jquery": "^3.7.1"
  }
}
